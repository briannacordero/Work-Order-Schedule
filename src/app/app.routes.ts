import { Routes } from '@angular/router';
import { WorkOrderSchedulePageComponent } from './features/work-order-schedule/work-order-schedule.page';

export const routes: Routes = [
  { path: '', component: WorkOrderSchedulePageComponent },
];
